#pragma once

// Renders the daylight tracker — gradient bg from sun altitude, invisible-arc
// sun position, sunrise/sunset times, narrative message with mixed weights,
// and a city label. Uses Oslo coordinates from config.h.
void view_daylight_render();
